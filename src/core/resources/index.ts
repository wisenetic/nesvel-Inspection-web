// src/core/resources/index.ts
// Central place to expose Refine resources derived from App modules.

import { appResources as moduleResources } from "@/core/bootstrap";

export const appResources = moduleResources;
